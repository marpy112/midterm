# ---- App ----
PORT=3000
# Used when running the app directly (npm start), e.g. on the VM:
MONGODB_URI=mongodb://127.0.0.1:27017/crud_demo

#  Docker Compose o

MONGO_ROOT_USER=appuser
MONGO_ROOT_PASSWORD=change-me
MONGO_DB=crud_demo
APP_PORT=3000
